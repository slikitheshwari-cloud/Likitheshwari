#include <stdio.h>

int main()
{
    int num,temp,digit,sum=0;
    printf("\n enter number: ");
    scanf("%d",&num);
    temp=num;
    while(num>0)
    {
        digit=num%10;
        sum=sum+(digit*digit);
        num=num/10;
        if(num==0&&sum>9)
        {
            num=sum;
            sum=0;
        }
    }
    if(sum==1)
    {
        printf("\n the given number is happy");
        
    }
    else
    {
        printf("\n given number is not happy");
    }
    return 0;
}